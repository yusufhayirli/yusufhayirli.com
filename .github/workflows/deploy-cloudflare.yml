name: Deploy to Cloudflare Pages

on:
  workflow_dispatch:

jobs:
  disabled:
    if: false
    runs-on: ubuntu-latest
    steps:
      - run: echo "Deployment is disabled. Build is handled by CI after merge."
