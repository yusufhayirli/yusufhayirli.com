/** @type {import('next').NextConfig} */
const nextConfig = {
  agentRules: false,
  output: "export",
  reactStrictMode: true,
  poweredByHeader: false,
  trailingSlash: true
};

export default nextConfig;
